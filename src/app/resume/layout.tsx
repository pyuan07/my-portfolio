export const metadata = {
  title: "Resume - Pin Yuan",
  description: "Professional background, experience, and skills of Pin Yuan.",
};

export default function ResumeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
